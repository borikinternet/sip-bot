# C1 evidence redaction

The evidence contains only local build paths, package versions, hashes, command results and PJSIP runtime diagnostics.
No SIP credentials, external addresses, user payloads, audio recordings or PBX information were collected.
