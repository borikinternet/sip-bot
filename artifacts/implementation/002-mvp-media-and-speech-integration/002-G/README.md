# 002-G execution evidence

Deterministic G-scope выполнен без запуска Ollama, загрузки модели и GPU inference. Здесь находятся evidence typed
facade, HTTP/NDJSON mapping, timeout/cancellation/stale-result tests и handoff для main-only real-provider gate.
