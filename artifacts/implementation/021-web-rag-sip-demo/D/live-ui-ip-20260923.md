# 021-D live local UI/IP evidence — 2026-09-23

Статус: `pass` для UI/QR/LAN configuration; финальный conference URL остаётся заменяемым.

## Current local addresses

- Page/QR: `http://192.168.1.74:8080/`.
- Windows web sidecar: `0.0.0.0:8080`.
- Browser WSS: `wss://192.168.1.74:7443`.
- FreeSWITCH mirrored WSS: `192.168.1.74:7443`.
- SIP realm and queue target: `192.168.1.74`, `sip:7100@192.168.1.74` (`science-bot@default`).

The QR asset `frontend/assets/qr-demo-ip.png` encodes exactly `http://192.168.1.74:8080/`.

## Checks

- `http://127.0.0.1:8080/` → `200`;
- `http://192.168.1.74:8080/` → `200`;
- `/assets/qr-demo-ip.png` → `200`, 400 bytes;
- Chromium loaded the page from the LAN host URL and observed:

  ```text
  title: Василиса — конференционный SIP-бот
  callerId: demo-e0ff9a5c78381f
  corpusState: Базовый корпус
  callDisabled: false
  qrSrc: /assets/qr-demo-ip.png
  qrMessage: Сканируйте, чтобы открыть локальный demo: http://192.168.1.74:8080/
  wsUrl: wss://192.168.1.74:7443
  targetUri: sip:7100@192.168.1.74
  ```

The current page configuration uses the local lab SIP credentials approved for this conference demo and keeps the
session-generated caller ID in the SIP URI user-part. The self-signed FreeSWITCH certificate is still a local-demo
limitation; a phone browser may require one-time certificate acceptance, while a production/public URL requires a
trusted certificate.
